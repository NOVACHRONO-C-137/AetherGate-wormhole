cd /mnt/d/syllabus/AetherGate/2-contracts-solana

cat > Cargo.toml << 'EOF'
[workspace]
members = [
    "programs/aether_gate"
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
EOF